imgui.ini
editor_prefs.ini
preferences.ini

# Bake output — regenerated by `OctarineEngine <project> -m bake`, never committed.
# Until OCTARINE_SHIPPED gating lands, a committed manifest would silently freeze dev `-p` runs.
asset_manifest.lua